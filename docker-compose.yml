version: '3.8'

services:
  redis-cache:
    image: redis:latest
    ports:
      - "6379:6379"

  python-app:
    build: .
    environment:
      - REDIS_HOST=redis-cache
      - EVICTION_POLICY=allkeys-lfu
      - MAX_MEMORY=5mb
      - TTL_Q1=10
    depends_on:
      - redis-cache